spring:
  application:
    name: web
  zipkin:
    enabled: false
  sleuth:
    enabled: false
server:
  port: 8080
logging:
  level:
    org:
      springframework:
        cloud:
          sleuth: DEBUG
